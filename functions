# Shell functions. {{{1
# © 2013 Tom Vincent <http://tlvince.com/contact>
# vim: ft=sh fdm=marker

# Create directory and enter it. {{{1
mkc() {
  mkdir -p "$@" && cd "$1"
}

# Spellchecker using aspell. {{{1
spell() {
  echo "$@" | aspell -a | grep -v "^@(#)"
}
